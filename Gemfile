source "https://rubygems.org"

# The exact gem bundle GitHub Pages builds with, so a local `jekyll serve`
# matches what actually gets deployed.
gem "github-pages", group: :jekyll_plugins

# Needed on newer Ruby versions; github-pages doesn't declare it itself.
gem "webrick"
